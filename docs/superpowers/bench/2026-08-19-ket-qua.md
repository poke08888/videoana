# Bước 0 — Kết quả bench, 19/08/2026

Ảnh thật: 2 SKU Lion Bartender (`DSC02167` Chill Time trắng-trên-xanh, `DSC02178` Party Up
vàng-kim-trên-đen). 8 clip 8s 9:16, cùng bối cảnh và prompt. Tổng $6,802 ≈ 177.000 đ.

Báo cáo có clip phát được: https://claude.ai/code/artifact/5344b9fb-0805-4e85-a8e6-77e8ce4572a7

## Điểm chữ nhãn trung bình (gộp 2 SKU, thang 1–5)

| Cấu hình | Giá/clip | Chữ nhãn |
|---|---|---|
| **Lite · low** | $0,40 | **4,00** |
| Fast · medium | $1,20 | 2,88 |
| Lite · medium | $0,40 | 2,25 |

**Kết luận: tiền không mua được chữ đúng, chỉ biên độ chuyển động mới mua được.**
Lite ở biên độ thấp ($0,40) thắng Fast ở biên độ trung bình ($1,20). Chữ vỡ theo kích thước
hiển thị trên khung — hai dòng nhỏ nhất (`DEODORANT`, `HEAT - TRIGGER`) luôn vỡ trước;
dòng lớn nhất và dòng đóng khung dưới cùng sống sót tốt nhất.

Hai SKU vỡ theo hai kiểu: nhãn trắng-trên-xanh **lặp dòng** (CHILL TIME × 3, "CHILUTIME");
nhãn vàng-trên-đen **nhào chữ** ("BEODOIDENT", "BERZ - TRIGGER").

Bàn tay trung bình 4,8/5 — rủi ro "bàn tay dị dạng" ở spec mục 12 KHÔNG xảy ra.
Hai lượt Fast cùng prompt cho kết quả lệch hoàn toàn (một lượt quay ra mặt sau, bịa chữ
thành phần) → chốt chặn "sinh 2 lượt chọn 1" là bắt buộc, không phải tối ưu hoá.

## Cấu hình chốt

```bash
STUDIO_VIDEO_MODEL_DRAFT=veo-3.1-lite-generate-preview
STUDIO_VIDEO_MODEL_FINAL=veo-3.1-fast-generate-preview
```

Kèm ràng buộc đã vào code (`ff2d3a0`): `motion_level` bị ép về `low` cho
`purpose ∈ {packaging, label}` trong `validateScript`.

## Ba lỗi bench bắt được

1. `6069e09` — `generateAudio` không được Gemini API hỗ trợ (chỉ Vertex AI). Mọi request
   Veo bị từ chối; tính năng clip sẽ chết hoàn toàn ở prod. Unit test không bắt được.
2. `ff2d3a0` — chốt chặn `motion_level=low` mà spec dựa vào chỉ tồn tại dưới dạng gợi ý
   trong prompt gửi AI, **chưa hề có trong code**.
3. `ff2d3a0` — prompt khẳng định nội dung nhãn sẽ ĐÈ LÊN ảnh tham chiếu. Gặp thật: prompt
   ghi "CHILL TIME" làm ảnh khoá bản Party Up in sai tên biến thể. Nguy hiểm vì `script.ts`
   để Gemini tự sinh `image_prompt` — đọc sai một chữ là hỏng cả lô mà không ai phát hiện.

## Chưa làm

- Bench Kling 3.0 Pro (thủ công trên kling.ai, cùng 2 ảnh khoá, 2 lượt/SKU).
- Đối chiếu giá thật với hoá đơn — Veo tự sinh audio, đơn giá có thể khác bảng.
- Cân nhắc đè lại vùng nhãn bằng ảnh gốc ở khâu ghép ffmpeg cho cảnh cận cần chữ sắc nét
  (chưa có trong thiết kế, cần phiếu riêng).
